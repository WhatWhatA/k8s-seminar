apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: alpine
spec:
  template:
    spec:
      metadata:
        name: alpine
      containers:
      - name: alpine
        image: alpine:latest
        args:
        - sleep
        - "1000"
